from Rtti import RttiInfofrom Rtti import RttiScannerfrom Exceptions.Types import *import ProductDecoderRttiInfo = reload(RttiInfo)RttiScanner = reload(RttiScanner)ProductDecoder = reload(ProductDecoder)class MsvcInfo :	def __init__(self) :		print "Checking whether I can find Ms tools signature..."		self.products = ProductDecoder.decode()		if self.products :			print "The following MS tools were used to build the binary:"			for p in self.products :				print p		else :			print "The binary was not built by MS tools or signatures were stripped."		self.rtti = RttiInfo.RttiInfo()	# End of __init__()			def scan(self) :		print "Checking whether there is any RTTI info I can use..."		RttiScanner.scan(self.rtti)	# End of scan()	# End of MsvcInfo